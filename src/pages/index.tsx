import { signIn, signOut, useSession } from "next-auth/react";
import Head from "next/head";
import { useRouter } from "next/router";
import { motion } from "framer-motion";
import { api } from "@/utils/api";
import InputQuestionExample from "@/components/InputQuestionExample";
import { FaArrowRight } from "react-icons/fa6";
import Image from "next/image";

export default function Home() {
  const { data: session, status } = useSession();

  const hello = api.post.hello.useQuery({ text: "from tRPC" });

  const router = useRouter();

  console.log(session?.user.email);

  return (
    <>
      <Head>
        <title>Chornia</title>
        <meta name="description" content="Generated by create-t3-app" />
        <link rel="icon" href="/favicon.ico" />
      </Head>
      <main className="flex min-h-screen flex-col items-center justify-center gap-4 px-3 text-center">
        <div className="flex w-full flex-col items-center justify-center gap-5 text-center lg:w-[450px] ">
          <h1 className="relative text-5xl lg:text-6xl">
            Improve your precious <br />
            <Image
              src="/svgs/lines.excalidraw.svg"
              alt="lines"
              width={40}
              height={40}
              className="absolute -left-4 -top-7"
            />
            <span className="font-bold text-blue-600 ">
              mental{" "}
              <span className="relative rotate-3 bg-blue-100 text-blue-600">
                health{" "}
                <img
                  src="/svgs/diamond.png"
                  className="absolute h-[50px] w-[50px] -top-6 -right-6"
                  alt=""
                />
              </span>
            </span>{" "}
          </h1>
          <p className="text-neutral-600 lg:text-[16px]">
            Explore your personal life areas with help of the{" "}
            <span className="font-bold text-neutral-900">AI help</span> ⚡️{" "}
            <br /> Write, reflects and grow --- Start healing now!
          </p>

          <ul className="flex flex-col items-start justify-start gap-2 text-sm">
            <li>✅ To yourself, you aren't judged</li>
            <li>✅ Implement Journaling Habit</li>
            <li>✅ GPT-4 Natural lenguage</li>
          </ul>
        </div>
        {/* <div className="flex flex-col gap-1">
          <p>Ask 3 months yourselft</p>
          <InputQuestionExample />
          <motion.button
            initial={{ scale: 1 }}
            whileHover={{ scale: 1.1, rotate: "0.3deg" }}
            whileTap={{ scale: 0.7 }}
            className="next-button"
          >
            Answer <FaArrowRight />
          </motion.button>
        </div> */}
      </main>
    </>
  );
}
